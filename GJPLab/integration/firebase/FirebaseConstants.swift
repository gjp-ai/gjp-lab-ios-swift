enum FirebaseConstants {
    static let appStarted = "app_started"
    // Firebase reserves the firebase_, google_, and ga_ prefixes for system events.
    static let featureOpened = "feature_firebase_opened"
    static let maintenanceEnabled = "gjp_lab_maintenance_enabled"
    static let performanceTrace = "firebase_demo_trace"
    static let demoTopic = "gjp_lab_demo"
}
